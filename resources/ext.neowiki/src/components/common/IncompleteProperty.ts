/**
 * A property definition the wiki will refuse to store: the property, and the
 * message naming what it still needs.
 */
export interface IncompleteProperty {
	propertyName: string;
	message: string;
}
